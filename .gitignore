# forget everything below!

# don't save large sequence files to GitHub
data/*

# we don't need the err and out files generated by slurm (just make sure slurm outputs to a directory called "slurm" located in the scripts directory
scripts/slurm/*.out
scripts/slurm/*.err

